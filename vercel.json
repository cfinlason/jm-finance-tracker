{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 --branch 3.44.3 _flutter && export PATH=\"$PATH:$(pwd)/_flutter/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release",
  "outputDirectory": "build/web",
  "installCommand": "echo 'Skipping install - handled in buildCommand'"
}
